import { StyleSheet, View } from 'react-native';import { ThemedScreen } from '@/components/layout/themed-screen';import { LearnScreenContextProvider, useLearnScreenContext } from '@/features/learn/context/pantry-screen-context';import React from 'react';import { LearnMainScreen } from '@/features/learn/screens/learn-main';import { LearnDialInScreen } from '@/features/learn/screens/learn-dial-in';import { LearnMilkScreen } from '@/features/learn/screens/learn-milk';import { LearnBeansScreen } from '@/features/learn/screens/learn-beans';import { LearnProps, RootStackParamList } from "@/navigation/types";import { RouteProp } from "@react-navigation/core";import { NativeStackScreenProps } from "react-native-screens/lib/typescript/native-stack/types";export function LearnCoreScreen({ route, navigation }: LearnProps) {    return (        <LearnScreenContextProvider>            <LearnCore route={route}/>        </LearnScreenContextProvider>    );}function LearnCore({ route }:  {route: RouteProp<RootStackParamList, 'learn'>}) {    const { screen } = useLearnScreenContext();    const { enterDir } = route.params;    return (        <ThemedScreen enterDir={enterDir} >            <View style={styles.container}>                {{                    'main': <LearnMainScreen />,                    'dial-in': <LearnDialInScreen />,                    'milk': <LearnMilkScreen />,                    'beans': <LearnBeansScreen />,                }[screen]}            </View>        </ThemedScreen >    );}const styles = StyleSheet.create({    container: {},});