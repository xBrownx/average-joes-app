import { StyleSheet, View } from 'react-native';export function LearnBeansScreen() {    return (        <View style={styles.container}>        </View>    )}const styles = StyleSheet.create({    container: {    }})