import Ionicons from '@expo/vector-icons/Ionicons';import React, { useEffect, useState } from 'react';import {    Button,    LayoutAnimation,    StyleSheet,    TouchableWithoutFeedback,    View,} from 'react-native';import Animated, {    FadeIn,    LinearTransition,    SlideInRight,    SlideOutRight,} from 'react-native-reanimated';import { ThemedDropdown, DropdownData } from '@/components/dropdown';import { ThemedText } from '@/components/text/themed-text';import { themedColors } from '@/constants';import { FormState, FormStateAction } from '@/features/recipes/machines/types';import { useAppSelector } from '@/store';import { selectRemoteMachines } from '@/store/slice/remote-data-slice';import { serverMachinesToDropdown } from '@/usecase';interface SearchMachineState {    makesList: DropdownData[];    selectedMake: string;    modelsList: DropdownData[];    selectedModel: string;}type SearchMachineProps = {    formState: FormState;    updateState: (name: FormStateAction, value: any) => void;};export function SearchMachine({ formState, updateState }: SearchMachineProps) {    const allMachines = useAppSelector(selectRemoteMachines);    const [state, setState] = useState<SearchMachineState>({        makesList: [],        modelsList: [],        selectedMake: '',        selectedModel: '',    });    const onMakeSelect = (id: string) => {};    const onModelSelect = (id: string) => {};    const onSave = () => {        updateState('isSearch', false);    };    useEffect(() => {        setState((prevState) => ({            ...prevState,            selectedRoaster: '',            selectedRoasterId: '',            selectedBlendId: '',            blends: [],        }));    }, []);    useEffect(() => {        LayoutAnimation.easeInEaseOut();    }, [state]);    return (        <TouchableWithoutFeedback>            <Animated.View                entering={SlideInRight.delay(50)}                exiting={SlideOutRight}            >                <Animated.View                    layout={LinearTransition}                    style={styles.modalInner}                >                    <View style={styles.titleContainer}>                        <ThemedText type={'subtitle'}>                            Search For a Bean                        </ThemedText>                        <Ionicons.Button                            name="arrow-back"                            size={24}                            backgroundColor={'transparent'}                            color={themedColors.tertiary}                            onPress={() => updateState('isSearch', false)}                        />                    </View>                    <ThemedText type={'default'} style={styles.titleContainer}>                        Add your bean profile, or use the search tool above.                    </ThemedText>                    <View style={styles.content}>                        <ThemedDropdown                            placeholder="Roaster"                            data={state.makesList}                            onChange={(value: string) => onMakeSelect(value)}                        />                        {state.modelsList && state.modelsList.length > 0 && (                            <Animated.View entering={FadeIn}>                                <ThemedDropdown                                    onChange={(value) => onMakeSelect(value)}                                    data={state.modelsList}                                    placeholder="Blend Name"                                />                            </Animated.View>                        )}                        <Animated.View layout={LinearTransition}>                            <Button                                title={'SAVE'}                                color={themedColors.primary}                                onPress={onSave}                            />                        </Animated.View>                    </View>                </Animated.View>            </Animated.View>        </TouchableWithoutFeedback>    );}const styles = StyleSheet.create({    titleContainer: {        flexDirection: 'row',        alignItems: 'center',        marginBottom: 16,        justifyContent: 'space-between',    },    content: {        gap: 8,    },    modalInner: {        width: '100%',        paddingHorizontal: 16,        paddingVertical: 12,        backgroundColor: 'white',        borderRadius: 8,    },});