import Ionicons from '@expo/vector-icons/Ionicons';import React, { useEffect, useState } from 'react';import {    Button,    LayoutAnimation,    StyleSheet,    TouchableWithoutFeedback,    View,} from 'react-native';import Animated, {    FadeIn,    LinearTransition,    SlideInRight,    SlideOutRight,} from 'react-native-reanimated';import { ThemedDropdown, DropdownData } from '@/components/dropdown';import { ThemedText } from '@/components/text/themed-text';import { themedColors } from '@/constants/themed-colors';import { FormState } from '@/features/kitchen/machines/types';import { useAppSelector } from '@/store';import { selectRemoteMachineMake, selectRemoteMachineModel } from '@/store/slice/remote-data-slice';import { remoteMachineMakeToDropdown, remoteMachineModelToDropdown } from '@/usecase';import { StateType, useCustomState } from '@/hooks/useCustomState';import { filterModelsToDropdown, filterModelsWithMakeId } from '@/features/kitchen/recipes/usecase';import { useMachineSearch } from "@/features/kitchen/hooks/useMachineSearch";interface SearchMachineState {    selectedMake?: string;    selectedModel?: string;}type SearchMachineProps = {    updateParentState: (state: StateType<FormState>) => void;};export function SearchMachine({updateParentState}: SearchMachineProps) {    const { makeDropdown, modelDropdown, filterModels } = useMachineSearch()    const {state, updateState} = useCustomState<SearchMachineState>({        selectedMake: '',        selectedModel: '',    });    const onMakeSelect = (machineId: string) => {        if (machineId === '') return        filterModels(machineId);        updateState({selectedMake: machineId});    };    const onModelSelect = (id: string) => {        if (id === '') return        updateState({selectedModel: id});    };    const onSave = () => {        updateParentState({isSearch: false});    };    useEffect(() => {        updateState({            ...state,            selectedMake: '',            selectedModel: '',        });    }, []);    return (        <TouchableWithoutFeedback>            <Animated.View                entering={SlideInRight.delay(50)}                exiting={SlideOutRight}            >                <Animated.View                    layout={LinearTransition}                    style={styles.modalInner}                >                    <View style={styles.titleContainer}>                        <ThemedText type={'subtitle'}>                            Search For a Bean                        </ThemedText>                        <Ionicons.Button                            name="arrow-back"                            size={24}                            backgroundColor={'transparent'}                            color={themedColors.tertiary}                            onPress={() => updateParentState({isSearch: false})}                        />                    </View>                    <ThemedText type={'default'} style={styles.titleContainer}>                        Add your bean profile, or use the search tool above.                    </ThemedText>                    <View style={styles.content}>                        <ThemedDropdown                            placeholder="Make"                            data={makeDropdown}                            value={state.selectedMake ?? null}                            onChange={(value: string) => onMakeSelect(value)}                        />                        {modelDropdown.length > 0 && (                            <Animated.View entering={FadeIn}>                                <ThemedDropdown                                    placeholder="Model"                                    data={modelDropdown}                                    value={state.selectedModel ?? null}                                    onChange={(value) => onModelSelect(value)}                                />                            </Animated.View>                        )}                        <Animated.View layout={LinearTransition}>                            <Button                                title={'SAVE'}                                color={themedColors.primary}                                onPress={onSave}                            />                        </Animated.View>                    </View>                </Animated.View>            </Animated.View>        </TouchableWithoutFeedback>    );}const styles = StyleSheet.create({    titleContainer: {        flexDirection: 'row',        alignItems: 'center',        marginBottom: 16,        justifyContent: 'space-between',    },    content: {        gap: 8,    },    modalInner: {        width: '100%',        paddingHorizontal: 16,        paddingVertical: 12,        backgroundColor: 'white',        borderRadius: 8,    },});