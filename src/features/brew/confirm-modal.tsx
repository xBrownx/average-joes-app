import { StyleSheet, View } from 'react-native';import { ThemedModal, ThemedModalProps } from '@/components/layout/themed-modal';import { useNavigation } from '@react-navigation/native';import { DrawerNavigationProp } from '@react-navigation/drawer';import { ThemedText } from '@/components/text';import { ThemedButton } from '@/components/button';export function ConfirmModal({ isOpen, onClose }: ThemedModalProps) {    const navigation = useNavigation<DrawerNavigationProp<any>>();    const navigateTo = (page: string) => {        onClose && onClose();        navigation.navigate(page as never);    };    return (        <ThemedModal isOpen={isOpen} onClose={onClose} title={'Where To?'} >            <View style={styles.container} >                <View style={styles.optionWrapper} >                    <ThemedText style={styles.label}>                        Been here before?                    </ThemedText >                    <ThemedButton                        type={'dark'}                        style={{ borderRadius: 20 }}                        onPress={() => navigateTo('brew')}                    >                        BREW                    </ThemedButton >                </View >                <View style={styles.optionWrapper} >                    <ThemedText style={styles.label}>                        Need a hand?                    </ThemedText >                    <ThemedButton                        type={'dark'}                        style={{ borderRadius: 20 }}                        onPress={() => navigateTo('dial-in')}                    >                        DIAL IN                    </ThemedButton >                </View >            </View >        </ThemedModal >    );}const styles = StyleSheet.create({    container: {        width: '100%',        alignItems: 'center',        gap: 24,        paddingHorizontal: 24,    },    optionWrapper: {        alignItems: 'center',        width: '100%',    },    label: {        color: 'black'    }});