import { StyleSheet, Text, View } from 'react-native';import { ComponentProps } from 'react';import { DragndropStartPoint } from '@/components/drag-drop/start-point';import { ThemedText } from "@/components/text";interface RectProps extends ComponentProps<typeof View> {    text: string;}export default function Selection() {    return (        <View style={styles.container} >            <Rect text={'CAPPUCCINO'} />            <Rect text={'LATTE'} />            <Rect text={'ESPRESSO'} />            <Rect text={'LONG BLACK'} />            <Rect text={'ICED LATE'} />        </View >    );}export const Rect = ({ text, style }: RectProps) => {    return (        <DragndropStartPoint data={{ color: '#EBD4B2', text }} >            <View style={styles.wrapper}>                <ThemedText                    type={'defaultSemiBold'}                    style={styles.text}                >                    {text}                </ThemedText >            </View >        </DragndropStartPoint >    );};const styles = StyleSheet.create({    container: {        flexDirection: 'row',        gap: 34,        rowGap: 18,        flexWrap: 'wrap',        alignItems: 'center',        justifyContent: 'center',    },    wrapper: {        backgroundColor: '#EBD4B2',        height: 33,        width: 133,        alignItems: 'center',        justifyContent: 'center',        borderWidth: 1,        borderColor: '#FFF',        borderRadius: 4,    },    text: {        fontSize: 13,    },});