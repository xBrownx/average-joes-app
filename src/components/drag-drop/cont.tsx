import { DragndropContextProvider } from '@/components/drag-drop/context';import { GestureHandlerRootView } from 'react-native-gesture-handler';import { Drag } from '@/components/drag-drop/drag';import { PropsWithChildren } from 'react';import { SafeAreaView } from 'react-native-safe-area-context';import { StyleSheet, View } from 'react-native';import Target from "@/components/drag-drop/taget";import Selection from '@/components/drag-drop/selection';export function Dnd({ children }: PropsWithChildren) {    return (        <GestureHandlerRootView style={{ flex: 1, }}>            <DragndropContextProvider >                <View style={{ flex: 1, gap: 61, flexDirection: 'column' }}>                    <Target />                    <Selection />                </View >            </DragndropContextProvider >        </GestureHandlerRootView >    );}const styles = StyleSheet.create({    container: {        width: '100%',        height: '100%',    },    text: {},});