import { useState } from 'react';import { Text, View, StyleSheet } from 'react-native';import { DragndropEndPoint } from '@/components/drag-drop/end-point';import { Data } from '@/components/drag-drop/context';import { ThemedText } from "@/components/text";export default function Target() {    return (        <View style={styles.container}>            <DropPoint idx={'1'} />            <DropPoint idx={'2'} />            <DropPoint idx={'3'} />            <DropPoint idx={'4'} />            <DropPoint idx={'5'} />        </View>    );}export const DropPoint = ({idx}: { idx: string }) => {    const [color, setColor] = useState('#6C6C6C');    const [text, setText] = useState('');    const onDrop = (data: Data) => {        setColor(data.color)        setText(data.text)    }    return (        <View style={styles.wrapper}>            <View style={styles.number}>                <ThemedText                    type={'defaultSemiBold'}                >                    {idx}                </ThemedText>            </View>            <DragndropEndPoint onDrop={(data) => onDrop(data)}>                <View                    style={[                        styles.boxContainer,                        {backgroundColor: color}                    ]}                >                    <ThemedText                        type={'defaultSemiBold'}                        style={styles.text}                    >                        {text}                    </ThemedText>                </View>            </DragndropEndPoint>        </View>    );};const styles = StyleSheet.create({    container: {        flex: 1,        gap: 16,        width: '100%',        alignItems: 'center',    },    wrapper: {        flexDirection: 'row',        gap: 8,        alignItems: 'center',        justifyContent: 'center',        paddingRight: 39,    },    number: {        backgroundColor: 'white',        width: 31,        height: 31,        borderRadius: 15,        justifyContent: 'center',        alignItems: 'center',    },    numText: {        lineHeight: 30,    },    boxContainer: {        width: 133,        height: 33,        justifyContent: 'center',        alignItems: 'center',        backgroundColor: '#6C6C6C',        borderWidth: 1,        borderColor: '#FFF',        borderRadius: 4,    },    text: {        fontSize: 13,    }})