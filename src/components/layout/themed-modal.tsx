import {    StyleSheet,    Modal as RNModal,    TouchableOpacity,    TouchableWithoutFeedback, View,} from 'react-native';import { THEME_COLOURS } from '@/constants';import { ModalProps as RNModalProps } from 'react-native/Libraries/Modal/Modal';import { ThemedText } from '@/components/text';import Animated, { LinearTransition, SlideInRight, SlideOutLeft } from "react-native-reanimated";export type ThemedModalProps = RNModalProps & {    title?: string;    noExit?: boolean;    isOpen?: boolean;    onClose?: () => void;};export function ThemedModal({ title, noExit, isOpen, onClose, children }: ThemedModalProps) {    return (        <RNModal            visible={isOpen ?? false}            transparent            animationType="fade"            statusBarTranslucent        >            <TouchableOpacity                style={styles.modalOuter}                onPress={noExit ? undefined : onClose}                disabled={noExit}            >                <TouchableWithoutFeedback >                    <Animated.View                        style={styles.modalInner}                        layout={LinearTransition}                    >                        <View style={styles.exitContainer}>                            {title &&                                <ThemedText type={'title'} style={styles.title}>                                    {title}                                </ThemedText>                            }                            {!noExit && <TouchableOpacity                                style={styles.exitEllipse}                                onPress={onClose ?? undefined}                            >                                <ThemedText style={{color: 'black'}}>                                    X                                </ThemedText>                            </TouchableOpacity>                            }                        </View>                        {children}                    </Animated.View >                </TouchableWithoutFeedback >            </TouchableOpacity >        </RNModal >    );}const styles = StyleSheet.create({    modalOuter: {        alignItems: 'center',        flex: 1,        justifyContent: 'center',        paddingHorizontal: 20,        paddingBottom: 40,        backgroundColor: 'rgba(0, 0, 0, 0.4)',    },    modalInner: {        width: '100%',        paddingHorizontal: 16,        paddingBottom: 30,        backgroundColor: THEME_COLOURS.tertiary,        borderRadius: 28,        alignItems: 'center',        justifyContent: 'center',    },    exitContainer: {        position: 'relative',        width: '100%',        alignItems: 'flex-end',        paddingVertical: 16,        height: 64,    },    title: {        position: 'absolute',        alignSelf: 'center',        marginTop: 16,        fontSize: 22,        lineHeight: 36,        color: 'black',    },    exitEllipse: {        backgroundColor: '#FFF6D6',        width: 32,        height: 32,        borderRadius: 16,        alignItems: 'center',        justifyContent: 'center',        paddingTop: 8,    },});