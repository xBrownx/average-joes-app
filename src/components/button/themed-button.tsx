import { THEME_COLOURS } from '@/constants/theme-colors';import { StyleSheet, type TextProps, TouchableOpacity } from 'react-native';import React from 'react';import { ThemedText } from '@/components/text/themed-text';type ThemedButtonProps = TextProps & {    children: string;    type?: 'primary' | 'secondary' | 'variant' | 'dark';    textType?: 'large' | 'small';    onPress?: () => void;    style?: any;}export function ThemedButton(    {        children,        type = 'primary',        textType = 'large',        onPress,        style,        ...rest    }: ThemedButtonProps) {    return (        <TouchableOpacity            onPress={onPress ? onPress : undefined}            style={[                styles.buttonContainer,                type === 'primary' ? styles.primary : undefined,                type === 'secondary' ? styles.secondary : undefined,                type === 'variant' ? styles.variant : undefined,                type === 'dark' ? styles.dark : undefined,                style,            ]}            {...rest}        >            <ThemedText type={'primaryBold'} style={[                type === 'primary' ? styles.primaryText : undefined,                type === 'secondary' ? styles.secondaryText : undefined,                type === 'variant' ? styles.secondaryText : undefined,                type === 'dark' ? styles.darkText : undefined,                textType === 'large' ? styles.large : undefined,                textType === 'small' ? styles.small : undefined,            ]}>                {children.toUpperCase()}            </ThemedText>        </TouchableOpacity>    );}const styles = StyleSheet.create({    buttonContainer: {        height: 33,        alignItems: 'center',        justifyContent: 'center',        borderRadius: 4,        borderColor: '#FFF',        borderWidth: 1,        width: '100%',    },    primary: {        backgroundColor: THEME_COLOURS.button.primary,    },    secondary: {        backgroundColor: THEME_COLOURS.button.secondary,    },    variant: {        backgroundColor: THEME_COLOURS.button.variant,    },    dark: {        backgroundColor: THEME_COLOURS.button.dark,    },    primaryText:{        color: 'black',    },    secondaryText:{        color: 'white',    },    darkText: {        color: THEME_COLOURS.tertiary,    },    large:{        fontSize: 18,        lineHeight: 36,    },    small:{        fontSize: 13,        lineHeight: 26,    },    buttonText: {        marginBottom: 0,        fontWeight: 'bold'    }});